[package]
name = "terrano-core"
description = "Raster algebra and terrain analysis — hillshade, slope, aspect, map algebra"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true

[dependencies]
thiserror = "2"
flate2 = "1"

[dev-dependencies]
criterion.workspace = true
